{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "WSL: Cargo Run (Windows .exe)",
      "type": "node-terminal",
      "request": "launch",
      "command": "cargo run",
      "cwd": "${workspaceFolder}"
    }
  ]
}
